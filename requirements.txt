# Development dependencies
# For full installation, use: pip install -e ".[all]"

# Testing
pytest>=7.0
pytest-cov>=4.0

# Type checking
mypy>=1.0

# Linting and formatting
ruff>=0.1.0

# Core dependencies (also in pyproject.toml)
pandas>=2.0
pyarrow>=14.0
rich>=13.0
